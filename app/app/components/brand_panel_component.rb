# Componente visual responsável por renderizar o painel de identidade visual (Branding).
# Geralmente utilizado em layouts de autenticação (Login/Cadastro) para exibir
# o logotipo, nome da aplicação ou imagem de destaque lateral.
#
# Atualmente não possui lógica de negócios (Ruby), servindo apenas como
# encapsulamento para o template HTML associado (brand_panel_component.html.erb).
class BrandPanelComponent < ViewComponent::Base
end