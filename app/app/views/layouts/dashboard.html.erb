<!DOCTYPE html>
<html>
  <head>
    <title>Camaar - Dashboard</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100 h-screen overflow-hidden">

    <% if flash[:notice] %>
      <div class="fixed top-0 left-1/2 transform -translate-x-1/2 mt-4 z-50 bg-green-200 text-green-800 p-3 rounded-lg shadow-lg">
        <%= flash[:notice] %>
      </div>
    <% end %>

    <div data-controller="sidebar" class="flex h-screen bg-gray-100 overflow-hidden">

      <%= render(Dashboard::SidebarComponent.new(user: current_user)) %>

      <div class="flex-1 flex flex-col min-w-0 overflow-hidden">

        <%= render(Dashboard::HeaderComponent.new(user: current_user, path: request.path)) %>

        <main class="flex-1 overflow-y-auto">
          <%# O CONTEÚDO DA VIEW (dashboard/index ou admins/dashboard) ENTRA AQUI %>
          <%= yield %> 
        </main>
      </div>
      </div>
      <turbo-frame id="modal"></turbo-frame> 
    </body>
</html>